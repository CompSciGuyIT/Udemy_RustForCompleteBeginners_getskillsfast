fn main() {
    let num:u8 = 255;
    let neg:i8 = -128;
    println!("{} is the maximum number for 8 bit unsigned integers.", num);
    println!("{} is the lowest number for 8 bit signed integers.", neg);
}
